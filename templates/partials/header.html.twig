<!--navbar-dark bg-primary-->
<nav class="navbar navbar-expand-lg bg-secondary" style="box-shadow: 0 0 4px 0 gray;">
    <img width="100px" style="margin-right:10px" src="/logocurieux.png">
    <a class="navbar-brand" href="#">Curieux.net</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path("homepage") }}">Accueil</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path("event_index") }}">Agenda</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path("place_index") }}">Lieux</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path("festival_index") }}">Festivals</span></a>
            </li>

        </ul>
        <ul class="navbar-nav ml-auto">

            {% if app.user %}

                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        <img src="{{ app.user.avatar}}" class="avatar avatar-mini" alt="Avatar de {{ app.user.fullName }} ">
                        {{ app.user.fullName }}
                    </a>
                    
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a class="dropdown-item" href="{{ path('event_create') }}">Annoncer un évènement</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('account_index') }}">Mon compte</a>
                        <a class="dropdown-item" href="{{ path('account_profile') }}">Modifier mon profil</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('account_logout') }}">Déconnexion</a>
                    </div>
                    
                </li>

            <!--
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('account_logout') }}">Déconnexion</span></a>
                </li>
            -->


            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('event_create') }}">Annoncer un évènement</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('account_register') }}">Inscription</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('account_login') }}">Connexion</span></a>
                </li>
            {% endif %}

        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}